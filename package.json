{
  "name": "quick-prosets",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:status": "found=$(for pid in $(pgrep -f \"$PWD/node_modules/.bin/vite\"); do lsof -nP -a -p \"$pid\" -iTCP -sTCP:LISTEN | awk 'NR>1 { sub(/.*:/, \"\", $9); print \"http://localhost:\" $9 \"  (pid \" $2 \")\" }'; done); [ -n \"$found\" ] && echo \"$found\" || echo 'No dev servers running for this project'",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tsconfig/svelte": "^5.0.0",
    "gh-pages": "^6.2.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^1.2.0"
  }
}
